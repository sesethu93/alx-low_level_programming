#include "main.h"
#include <stdio.h>
/**
 * main - thi function is to print alphabets im lowercase
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{
	char letter;

	for (letter = 'a' ; letter <= 'z' ; letter++)
		_putchar(letter);
	_putchar(letter);
}
